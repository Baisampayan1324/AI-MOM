{
  "version": 2,
  "name": "ai-mom-frontend",
  "builds": [
    { "src": "frontend/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ],
  "env": {
    "BACKEND_URL": "https://<your-render-backend-url>" 
  }
}
